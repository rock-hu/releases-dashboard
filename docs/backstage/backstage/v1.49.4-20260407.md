# v1.49.4

## release on 20260407
## description
## changes
This patch release fixes the following issues:

* Fix OAuth 2.0 Protected Resource Metadata endpoint returning wrong URL
* Fixed incorrect name of the <code>legacy-frontend-plugin</code> template
* Fix issue with missing permissions on the <code>/.well-known</code> endpoint for the scaffolder plugin

