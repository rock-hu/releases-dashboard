# v0.25.5

## release on 20260518
## description
## changes
Changes

Add <code>decision_log.dropped</code> counter for async decision logging (<a class="issue-link js-issue-link" data-error-text="Failed to load title" data-id="4431049202" data-permission-text="Title is private" data-url="https://github.com/zalando/skipper/issues/4007" data-hovercard-type="pull_request" data-hovercard-url="/zalando/skipper/pull/4007/hovercard" href="https://github.com/zalando/skipper/pull/4007">#4007</a>)

What & Why?

Follow-up to <a class="issue-link js-issue-link" data-error-text="Failed to load title" data-id="4405035201" data-permission-text="Title is private" data-url="https://github.com/zalando/skipper/issues/4001" data-hovercard-type="pull_request" data-hovercard-url="/zalando/skipper/pull/4001/hovercard" href="https://github.com/zalando/skipper/pull/4001">#4001</a>, which introduced a bounded async channel in front of  

<code>logDecision</code> so that slow log plugins (e.g. <code>eopa_dl</code> writing to S3)  

don't add latency to HTTP responses. When the channel is full, events  

are silently dropped.

Currently, those drops are only visible as warn log lines. This PR  

surfaces the same event as a Prometheus counter so we can:

* <strong>Alert</strong> when drops exceed a threshold (should we decide on one based  
  on operational experience)
* <strong>Tune <code>buffer_size_limit_events</code></strong> based on observed drop rate.

The counter is emitted via skipper's standard <code>metrics.Default</code>:

    skipper_custom_total{key="decision_log.dropped.<bundle>"}

to match the existing OPA counter convention (<code>decision.allow.&lt;bundle&gt;</code>,  

<code>decision.deny.&lt;bundle&gt;</code>, <code>decision.err.&lt;bundle&gt;</code>).

<em>Given naming is hard, please feel free to suggest a better name for the<br>
metric.</em>

Multiarch Docker image

Multiarch Docker image is available in GitHub's docker registry:

    docker run -it ghcr.io/zalando/skipper:v0.25.5 skipper --help

Docker image

Docker image is available in Zalando's Open Source registry:

    docker run -it registry.opensource.zalan.do/teapot/skipper:v0.25.5 skipper --help


