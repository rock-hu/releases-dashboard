# v0.24.83

## release on 20260505
## description
## changes
Changes

fix: fatal at startup, if we can not create a valkey connection (<a class="issue-link js-issue-link" data-error-text="Failed to load title" data-id="4351119445" data-permission-text="Title is private" data-url="https://github.com/zalando/skipper/issues/3988" data-hovercard-type="pull_request" data-hovercard-url="/zalando/skipper/pull/3988/hovercard" href="https://github.com/zalando/skipper/pull/3988">#3988</a>)

fix: fatal at startup, if we can not create a valkey connection, we  

should assume something is wrong, because we have this dependency

Multiarch Docker image

Multiarch Docker image is available in GitHub's docker registry:

    docker run -it ghcr.io/zalando/skipper:v0.24.83 skipper --help

Docker image

Docker image is available in Zalando's Open Source registry:

    docker run -it registry.opensource.zalan.do/teapot/skipper:v0.24.83 skipper --help


